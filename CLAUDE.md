# forum-dotnet — Working Memory (v0.2, scaffold + locked assumptions)

## What this is
**Architecture A** of the master's thesis: decoupled **React SPA + .NET 10** forum. Self-contained git repo
(backend + frontend + db + k8s + infra + scripts + CI). Benchmarked against **Architecture B** (colleague's
Go SSR monolith, `../gomx`). The colleague adapts to THIS spec — do not mirror gomx. Goal: professional,
deliberately slightly over-engineered, to showcase best practices. Authors: Jakub Patkowski (95818) &
Hubert Ożarowski (97692); supervisor Dr inż. Kamil Żyła.

## Authoritative design docs (read these; they win over any stale phrasing below)
- **`docs/architecture/REQUIREMENTS-AND-ASSUMPTIONS.md`** — master spec: the 10 locked assumptions + functional scope.
- **`docs/architecture/DOMAIN-MODEL-AND-DATABASE.md`** — full schema (ULID, audit, soft-delete, ownership, files→target, FTS, keyset).
- **`docs/architecture/IMPLEMENTATION-PLAN.md`** — phased build plan (Phases 0–10) with per-phase steps, gotchas, Definition of Done, and START-OF-PHASE REMINDERS. Re-read the relevant phase block before coding.
- **`docs/architecture/FOUNDATION-AND-SHARED-ABSTRACTIONS.md`** — Phase 0 result: every shared abstraction (SharedKernel / Common / Infrastructure / Api wiring) + the foundation assumptions every module inherits.
- **`docs/db/permissions-acl-design.md`** — RBAC + bitmask ACL resolved in SQL.
- **ADRs:** 0002 modular monolith · 0003 CQRS no MediatR · 0004 SQL ACL · 0005 migrations as k8s Job · **0006 ULID everywhere** · **0007 Argon2id** · **0008 direct-to-MinIO presigned uploads** · **0009 RabbitMQ integration events (outbox)** · **0010 WebSocket real-time (fetch-then-patch)**.
- Shared scope contract with B: `../../docs/specs/forum-spec.md` + `../../docs/architecture/PROPOZYCJA-UJEDNOLICENIA-A-B.md` — **neither file has actually been written yet** (confirmed absent from the repo and from git history on every branch, 2026-07-16); treat as real documentation debt, not a stale-lookup problem, until they exist. See `docs/architecture/POST-9C-ROADMAP.md` for current post-9c priorities (Redis scope, Social scope, what's blocked on Hubert).

## 10 locked assumptions (full detail in REQUIREMENTS-AND-ASSUMPTIONS.md)
1. Professional relational schema (modeled on forum-wędkarskie) + RBAC **and** bitmask ACL; global roles `user/moderator/admin` + per-context roles (category/group); ownership; soft-delete; full audit on every aggregate root.
2. **ULID everywhere** (no ints/GUIDv4 in URLs). 3. **Argon2id** hashing. 4. **Direct-to-MinIO** presigned uploads (bytes bypass backend).
5. **Module-first** monolith + **RabbitMQ** integration events via transactional **outbox** (no cross-module/-schema coupling). 6. **WebSocket** change-notifications on aggregate change; SPA fetches then patches in place.
7. Observability: Serilog→Loki + OTel→Tempo + Prometheus→Grafana; `/health/{live,ready}`. 8. Functional scope = the golden mean.
9. Reads via SQL views + **keyset**; writes via aggregates + UnitOfWork; Result pattern; **404→403→422**. 10. Migrations as a **k8s Job**, not at startup.

**Files→object rule:** every file links to exactly one target via `(target_type, target_id)` — the "FK to the object it's attached to" (logical ref, kept consistent by events; no cross-schema DB FK). Aggregate roots all carry `created_on_utc/created_by/last_modified_on_utc/last_modified_by`; owned ones add `owner_id`; removable ones add `is_deleted/deleted_on_utc/deleted_by`.

## Golden rules
- **English** for all code, identifiers, comments. Polish only for thesis prose/docs.
- **Module-first**: solution organized by business module, not layer. Each module = 1 project with `Domain/ Application/ Infrastructure/ Presentation/ Contracts/` folders. Everything `internal` except `Contracts/`; modules interact only via Contracts + integration events. `Forum.ArchitectureTests` enforces module boundaries AND in-module layer purity (Domain free of Infrastructure/framework).
- Logic lives in `Core.Application` use-case handlers; endpoints/consumers are thin. Handlers return `Result`/`Result<T>` — **no exceptions for expected failures**.
- Reads go through **SQL views + view repositories**; writes through **aggregates + repository + UnitOfWork**. Avoid N+1; prefer keyset pagination.
- Central Package Management only — **never** put `Version=` in a `.csproj`; add it to `backend/Directory.Packages.props`.
- Secrets: `dotnet user-secrets` (dev) / k8s Secret (cluster). Never in `appsettings*.json` or git.
- **Mount-lag caveat:** after writing a file in this sandbox, `cat` may show a stale/truncated copy — trust the Read tool / re-list, don't "fix" phantom truncation.
- **⚠️ GIT COMMITS AND PUSHES ARE FORBIDDEN.** Under NO circumstances may Claude make a git commit or push, regardless of what the prompt says. ALL commits and pushes MUST be manually executed by you. Claude must only stage changes and prepare them — you control all git history and remote operations. This is non-negotiable.

## Layout (13 projects, module-first)
`src/` has exactly three folders:
- **Bootstrap/** — `Forum.Api` (the only executable: Program.cs, auth, middleware, OTel, health, OpenAPI; discovers `IModule`s and wires them).
- **Shared/** — `Forum.SharedKernel` (Entity, AggregateRoot, Result, Error), `Forum.Common` (CQRS abstractions, `IModule`/`IEndpoint`, in-process event bus, paging, correlation), `Forum.Infrastructure` (shared adapter base: EF base, outbox, RabbitMQ, MinIO, startup tasks).
- **Modules/** — `Identity · Content · Files · Engagement` (each = 1 project `Forum.Modules.X` with `Domain/ Application/ Infrastructure/ Presentation/ Contracts/` + `XModule.cs`). `Social` confirmed next (Phase 5 — friends/DMs/groups, see Current state); `Notifications, Audit` added later. A `Modules/Directory.Build.props` gives every module the same baseline (frameworks + EF + validation + shared refs) so module csprojs stay tiny.

Tests: `ArchitectureTests · Modules.Identity.Tests · Modules.Content.Tests · IntegrationTests · TestUtilities`. Each module owns its **schema + DbContext + migrations**; cross-module refs go through `*.Contracts` only.

## Patterns (from a prior production .NET service, improved)
- **CQRS without MediatR**: `ICommand/IQuery` + handlers (`Forum.Common`), registered via **Scrutor** scan in `Core.Application`.
- **Result pattern**: `Result`, `Result<T>`, `Error`, `ErrorType` (`Forum.SharedKernel/Results`). One error→HTTP mapper at the REST edge → envelope `{ code, description, type }` (404→403→422 order).
- **Domain events**: aggregate `Raise(...)` → collected in DbContext `SaveChangesAndDispatchEventsAsync` → handlers in `Core.Application`. Cross-service events go through the **outbox** (`Adapters.Out.Messaging`), not in-process.
- **Audit interceptor** stamps created/modified by/on. **Ulid** keys. EF: `NoTracking` reads, `SplitQuery`, snake_case naming.
- **Minimal API `IEndpoint`**: 1 file = 1 endpoint, in a module's `Presentation/`, mapped by that module's `XModule.MapEndpoints`.
- **Startup tasks** (`Forum.Infrastructure/Startup`): ordered migration → views → seed. In k8s migrations run as a **Job**, not at startup (HPA race) — `Forum.Api` supports a `migrate` arg.
- **Module installer** (`XModule : IModule` in each module): registers DI + maps endpoints + owns the module's schema/migrations. `Forum.Api` holds the explicit module list.

## Security (from forum-wędkarskie, in .NET)
JWT access 15 m + refresh 14 d with **rotation + reuse-detection**; refresh in httpOnly cookie, access in JS memory.
**Argon2id** hashing. **RBAC + bitmask ACL resolved in SQL** — see `docs/db/permissions-acl-design.md` and ADR 0004
(int_or_agg aggregate, `effective_mask` resolver, `effective_perm_cache`, partial + BRIN indexes). Security headers,
rate limiting, explicit CORS allow-list, upload size limit — all in `Host`.

## Database
PostgreSQL 17, one DB (`forum_net`), **one schema + DbContext + migration chain PER MODULE**
(`forum_identity`, `forum_authz`, `forum_content`, `forum_files`, `forum_engagement`, `forum_social`). **No FK crosses a schema**
(cross-module links are logical ULIDs kept consistent via integration events). Views for read models; `tsvector` FTS;
**keyset** pagination; pooled DbContext + tuned Npgsql pool (replicas × pool ≤ max_connections).
ACL functions/aggregate/`int_or_agg`/`effective_mask()` ship as raw-SQL EF migrations in the Identity module's `Infrastructure/Acl/`.
Full schema: `docs/architecture/DOMAIN-MODEL-AND-DATABASE.md`.

## Observability
Serilog → Loki; OpenTelemetry traces → Tempo; metrics → Prometheus (`/metrics`); Grafana dashboards.
Correlation-Id middleware; `/health/live` + `/health/ready` (readiness gates DB/RabbitMQ).

## Commands
```bash
cd backend
dotnet restore Forum.slnx
dotnet build   Forum.slnx
dotnet test    Forum.slnx                  # unit + architecture + integration (Testcontainers → Docker required)
dotnet format  Forum.slnx                  # style (CI verifies --verify-no-changes)
dotnet run --project src/Bootstrap/Forum.Api
# per-module migrations (each module owns its DbContext):
dotnet ef migrations add <Name> -p src/Modules/Identity/Forum.Modules.Identity -s src/Bootstrap/Forum.Api -c IdentityDbContext
```
Cluster (Phase 10b): `make mk-up` (calico CNI + ingress + metrics-server) → `make mk-tls` (once; mkcert cert) → `make mk-deploy ARGS=--seed` (both images into minikube, secrets generate-if-missing, full apply order + Jobs) → `make tunnels` (admin/Windows access; Ctrl+C stops all). Load: `../scripts/run-load-test.sh smoke`. Windows browser/DataGrip access: `docs/runbooks/wsl-minikube-setup.md` §4.
Dev observability: **`/dev/monitor`** (Development only; `Forum.Api/DevTools/` — embedded single-file page + `/dev/monitor/bus` WS tap: passive exclusive `#`-bound queue on all module exchanges, never competes with real consumers) — live view of bus events AND the Phase 7 hub as a real client (login → ticket → subscribe, auto-reconnect), correlation-id tracing across both streams; needs local infra migrated (`dotnet run … migrate`).

## Build order (next work) — see `docs/architecture/IMPLEMENTATION-PLAN.md` for full per-phase detail
Phase 0 Foundation/build-green → 1 Identity+Authz → 2 Content → 3 Files → 4 Engagement →
5 Social (**CONFIRMED 2026-07-16** — friends+DMs+groups, all realtime via WS; see Current state) → 6 Messaging backbone (RabbitMQ+outbox) → 7 WebSocket → 8 Frontend →
9 Seed+benchmark+observability → 10 k8s deploy+hardening+comparative run.
Each phase block has Goal · Depends on · Steps · Watch out · Definition of Done · Events · START-OF-PHASE REMINDERS.
When the user says "finished phase X, start phase Y", re-read that phase block + the referenced docs first.

## Current state
**Phase 0 complete** (branch `feat/phase-0-foundation`) — `dotnet build`/`test`/`format` green on .NET 10, `/health/live` smoke test passes, ArchitectureTests green.
- **SharedKernel**: `Result`/`Error`, `Entity`, `AggregateRoot` (domain events + audit fields), `IAuditableEntity`/`IHasDomainEvents`/`ISoftDeletable`/`IOwned`.
- **Forum.Common**: CQRS markers, `IModule`/`IEndpoint`, `IEventBus`+`IIntegrationEventHandler`, paging, `ICorrelationContext`, `ICurrentActor`.
- **Forum.Infrastructure**: `ForumDbContext` base (no-tracking reads, soft-delete query filter, `SaveChangesAndDispatchEventsAsync`), `AuditInterceptor`, `DomainEventDispatcher`, `InMemoryEventBus`, `OutboxMessage`(+config), lazy `RabbitMqConnection`, `MinioObjectStorage`, `AddForumInfrastructure`, `ModuleDbContextRegistration` (snake_case + interceptor), `RunMigrationsAsync`.
- **Forum.Api**: correlation-id middleware + `CorrelationContext`, ProblemDetails exception handler, CORS allow-list, rate limiter, JWT bearer + authz skeleton, `migrate` arg hook wired in `Program.cs`.
- **Tests**: `ArchitectureTests` (boundary + Domain purity), `PostgresFixture` (Testcontainers), `HealthCheckTests` smoke test.
- Build hygiene applied: CPM fixed (no `--` in XML comments), NuGet-audit + a few CA/style rules tuned in `.editorconfig`/`Directory.Build.props`.

**Phase 1 — Identity + Authz: code-complete (2026-06-27).** Module `Forum.Modules.Identity` (Domain/Application/Infrastructure/Presentation/Contracts), registered in `Program.cs`.
- **`forum_identity`** (EF migration `InitialIdentity`): `users` (citext email, `username_lc` unique, **Argon2id** via Isopoh, status, audit) + `refresh_tokens` (family, SHA-256 hash, rotation chain). **`forum_authz`** (raw-SQL migration `AddAuthzSchema` in `Infrastructure/Acl/AuthzSchema.cs`): actions/roles/user_roles/acl_entries/effective_perm_cache, `int_or_agg`, `effective_mask()`/`has_permission()`/`recompute_user_perms()`, hot-path/partial/BRIN indexes, role+action seed.
- **Use cases** (Result): register, login (non-revealing + dummy verify), refresh (rotation + **family reuse-detection**), logout, logout-all, admin list/roles/ACL/status. Endpoints `/api/identity/*` + `/admin/users/*`, httpOnly refresh cookie, tighter auth rate-limit.
- **Shared authz surface** added to `Forum.Common`: `ICurrentUser`, `IPermissionService`, `RequirePermission` filter, `JwtOptions` (dev signing-key fallback), `RateLimitPolicies`, `ApiResults` (404→403→422). `CurrentUser` backs `ICurrentActor`; resolves `IPermissionService` lazily to avoid a DbContext↔AuditInterceptor DI cycle.
- **Verified green:** `dotnet build`, `dotnet format --verify-no-changes`, 15 Identity unit tests, ArchitectureTests.
- **Phase 1 DoD closed (2026-07-01):** Docker available again — `AclSqlTests` + `IdentityFlowTests` green under Testcontainers. `dotnet ef` 10.0.2 via tool manifest (`dotnet tool restore`).

**Phase 2 — Content: code-complete + fully verified (2026-07-01, branch `5-feat-phase-2---content`).** Module `Forum.Modules.Content` filled in (was a scaffold), already registered in `Program.cs`.
- **`forum_content`** — migration `InitialContent` (EF): `categories` (slug unique, visibility as text, owner, soft-delete, audit), `threads` (markdown raw, pinned, keyset index `ix_threads_feed (category_id, is_pinned DESC, created_on_utc DESC, id DESC) WHERE is_deleted=false`), `comments` (materialized `path` ≤161 chars, depth ≤ 5, `ix_comments_thread_path`), `tags`+`thread_tags`, `outbox_messages`. Migration `AddFtsAndViews` (raw SQL in `Infrastructure/Fts/ContentFtsAndViews.cs`): `search_tsv` **outside the EF model** + trigger (title A / body B, 'simple') + GIN, views `thread_feed_v` / `comment_tree_v` (keeps deleted rows) / `thread_detail_v` (tags as text[]); views JOIN `forum_identity.users` — view-level read join, Identity migrates first (module registration order). Migrations folder has its own `.editorconfig` (`generated_code=true`), same as Identity.
- **Domain:** `Category`/`Thread`/`Comment` aggregates (IOwned+ISoftDeletable; a **global using-alias** resolves the `Thread` vs `System.Threading.Thread` clash), `Comment.CreateReply` enforces `MaxDepth=5`, delete → body `"[deleted]"` keeping children; `Tag`+`ThreadTag`.
- **Use cases** (Result, 404→403→422 order in handlers): category CRUD, thread create (tags get-or-create)/update/delete/pin/change-category, comment create/update/delete, feed/search/tree/detail queries. **Permission mapping onto the Phase 1 action catalog** (prompt names like `thread.create`/`*.any` don't exist): create=`create`, comment=`comment`, moderator powers=`moderate` resolved **at category scope inside handlers** (covers global roles AND per-category `acl_entries`); ownership via `ICurrentUser.IsOwner`. Integration events in `Contracts/` (Category/Thread/Comment Created/Updated/Deleted) written via module-local `IOutboxWriter` (same as Identity); `UserBlockedEventHandler` consumer hooked (log-only until Phase 6).
- **Reads:** raw-ADO view queries in `ContentQueries`; keyset cursors `ThreadFeedCursor`/`ThreadSearchCursor` (pipe-delimited payload, Base64Url; search cursor carries `ts_rank`) — **no OFFSET**; FTS via `websearch_to_tsquery('simple')` (robust to raw user input; **no ILIKE**). 17 endpoint operations under `/api/content/*`, all visible in OpenAPI.
- **Guardrails updated:** `ModuleBoundaryTests` now permits cross-module `*.Contracts` while banning Domain/Application/Infrastructure/Presentation both ways; Content domain-purity rule added. Rate-limit permits configurable via `RateLimiting:{Global,Auth}:PermitLimit` (defaults 100/10) — `ForumApiFactory` raises them so suites don't 429.
- **Verified green (everything):** `dotnet build` · `dotnet format --verify-no-changes` · `dotnet test` = **57 tests** (26 Content unit, 20 Identity, 2 arch, 9 integration incl. 5 `ContentFlowTests` E2E: keyset paging without dupes + tags, depth-cap 422, `"[deleted]"` keeps children, FTS single hit, 403-for-plain-user vs moderator pin/delete + cursor across the pinned boundary).

**Phase 3 — Files: code-complete + fully verified (2026-07-03).** Module `Forum.Modules.Files` filled in (was a scaffold), already registered in `Program.cs`.
- **`forum_files`** — migration `InitialFiles` (EF): `files` (bucket+object_key unique, declared content_type/size_bytes, width/height, status text pending/committed, `uploaded_by` = `OwnerId` (IOwned) + full audit via AggregateRoot, **no soft-delete** — orphans are physically removed), `file_attachments` (PK file_id+target_type+target_id, `ix_attach_target`, cascade), `outbox_messages`; partial indexes `ix_files_pending_sweep`/`ix_files_committed_sweep` match the sweep predicates. Enums stored as text (Content precedent), NOT PG enums.
- **Flow (ADR 0008):** `POST /api/files` (initiate: any authenticated user, allow-list+max-size on declared values, pending row, ULID-month-sharded key `yyyy/MM/{ulid}`, presigned PUT) → client PUTs to MinIO → `POST /api/files/{id}/commit` (uploader-only; **stats real size + sniffs real type from magic bytes + decodes dimensions via hand-rolled `ImageProbe`** — PNG/JPEG/GIF/WebP header parsers, no ImageSharp: 4.x demands a paid build-time license key; declared values never trusted; idempotent re-commit). Reads: `GET /api/files/{id}` + `GET /api/files?targetType&targetId` — presigned GET, anonymous (public forum parity; short TTL). 6 endpoints, all in OpenAPI.
- **Attach authorization (key deviation from the "Content calls a Files port" idea — that direction would create a circular project reference, since Files already consumes Content's deletion events):** dependency stays one-way Identity ← Content ← Files. Files owns `POST/DELETE /api/files/{id}/attachments`; thread/comment/category_icon targets are gated by **Content's new Contracts surface `IContentAuthorization.AuthorizeAttachmentAsync`** (internal `ContentAttachmentAuthorizer`: existence 404 → owner-or-moderator-at-category 403, uses `IPermissionService` with explicit userId); avatar is self-authorized (target = current user), dm → 422 until Phase 5. Only the uploader may attach their file; avatar/category_icon get replace semantics, thread/comment additive with cap (`Files:MaxAttachmentsPerTarget`, 10). `users.avatar_file_id`/`categories.icon_file_id` sync deferred to Phase 6 (consume `FileCommitted`+attachments via relay).
- **Orphan sweep:** `OrphanSweeper` (Application, unit-testable) + `OrphanSweepService` **BackgroundService** (PeriodicTimer, first tick after one interval) — a recurring job, deliberately NOT `IStartupTask`; cross-replica dedupe via **Postgres session advisory lock** (`AdvisorySweepLock`, `ISweepLock` port). Removes blob-then-row (both idempotent): pending past `PendingGraceMinutes` (60) and committed-unattached past `UnattachedGraceMinutes` (1440), batch-capped; each removal enqueues `FileOrphanedIntegrationEvent`.
- **Events/consumers:** `FileCommitted`/`FileOrphaned` in `Contracts/IntegrationEvents` via module-local outbox writer; consumers for Content's `ThreadDeleted`/`CommentDeleted` detach attachments (real logic now, relay drives them in Phase 6; integration test invokes handler directly).
- **Shared infra extended:** `IObjectStorage` += `EnsureBucketAsync`/`PresignPutAsync`/`PresignGetAsync`/`StatAsync` (null when missing)/`ReadRangeAsync`/`RemoveAsync` (idempotent) + `ObjectStatResult`; `StorageOptions` untouched — Files policy lives in module-local `FilesOptions` ("Files" section in appsettings: TTLs, allow-list, max size 5 MiB, probe 128 KiB, graces, interval, batch, cap). `Testcontainers.Minio 4.1.0` pinned in CPM.
- **Guardrails:** `ModuleBoundaryTests` extended — Files may touch Identity/Content only via Contracts, upstream modules must not depend on Files at all, Files domain-purity rule. Files csproj has `InternalsVisibleTo` for its tests + `Forum.IntegrationTests` (drives sweeper/consumers until Phase 6).
- **Verified green (everything):** `dotnet build` · `dotnet format --verify-no-changes` · `dotnet test` = **104 tests** (40 Files unit incl. ImageProbe/StoredFile/sweeper/commit+attach handlers, 26 Content, 20 Identity, 2 arch, 16 integration incl. 7 `FilesFlowTests` E2E against real MinIO: presigned PUT→commit-with-dimensions→attach→presigned GET byte-identical→detach, size/type-mismatch 422s, commit-before-upload 409, foreign-thread attach 403, thread-delete→consumer-detach→sweep removes blob+row, abandoned pending swept). `ForumApiFactory` now also boots a MinIO container (parallel with Postgres), bootstraps the bucket via `EnsureBucketAsync`, zeroes both grace windows (sweep only runs when a test invokes it).

**Phase 4 — Engagement: code-complete + fully verified (2026-07-04, branch `9-feat-phase-4---engagement`).** Module `Forum.Modules.Engagement` filled in (was a scaffold), already registered in `Program.cs`.
- **`forum_engagement`** — migration `InitialEngagement` (EF): `reactions` (PK `(user_id, target_type, target_id, reaction_type)`; `reaction_type` text — only `'like'` implemented, but it sits in the key as the deliberate hook for future multi-kind reactions; `value smallint DEFAULT 1` is the SEPARATE signed-vote axis, extensible to -1; target_type text `thread|comment`, `ix_reactions_target`), `outbox_messages`. Migration `AddEngagementCountersAndViews` (raw SQL in `Infrastructure/Counters/EngagementCountersAndViews.cs`): **`reaction_counts` lives OUTSIDE the EF model and is maintained ONLY by an `AFTER INSERT OR DELETE` row trigger** (mirrors the `search_tsv` trigger precedent — counter can't drift no matter which code path writes: toggle, cascade consumers, future seeding; zeroed rows are deleted) + **`user_stats_v`** read-only cross-schema view (Engagement migrates last): live thread/comment counts + karma = `SUM(r.value)` over reactions on the user's live content.
- **Content untouched** (its views still show `0 AS like_count` placeholders — deliberate): no upstream schema/view/project-ref changes. Engagement resolves the like gate itself via `ContentTargetReader` — read-only ADO into `forum_content.threads/comments/categories` (the "later module read-joins earlier tables" precedent). Gate order mirrors CreateThread: target 404 → private-category owner-or-`moderate` 403 → **`Permissions.Like` at category scope** 403 (first use of the Phase 1 `like` bit). SPA composes feed + `/reactions/batch` itself (fetch-then-patch pattern).
- **Use cases** (Result): AddReaction/RemoveReaction — **idempotent both directions** (re-like / un-like-never-liked are 200 no-ops returning current `{count, viewerReacted}`), `ReactionAdded/Removed` via module-local outbox writer; GetReactionSummary + batch (≤100 ids, one round-trip two-resultset ADO query, zero-fill for unknown ids — **no existence check on reads by design**, keeps them pure PK lookups); GetUserStats (404 unknown user). 5 endpoints under `/api/engagement/*` (PUT/DELETE/GET `/reactions/{targetType}/{targetId}`, GET `/reactions/batch`, GET `/users/{id}/stats`; reads anonymous), all verified in OpenAPI against a live host. Consumers for `ThreadDeleted`/`CommentDeleted` bulk-`ExecuteDelete` reactions (row trigger folds counters; delete-then-consume drift window closes via relay in Phase 6).
- **Guardrails:** `ModuleBoundaryTests` extended — Engagement touches Identity/Content only via Contracts, must not touch Files, nobody may depend on Engagement, domain-purity rule. **`FilesFlowTests` consumer invocation switched to `GetServices<…>` loop** (drives ALL handlers like the bus): `GetRequiredService` would now resolve Engagement's `ThreadDeleted` handler, since it registers last.
- **Verified green (everything):** `dotnet build` · `dotnet format --verify-no-changes` · `dotnet test` = **134 tests** (24 Engagement unit incl. toggle idempotency/gate order/batch validation/consumers, 40 Files, 26 Content, 20 Identity, 2 arch, 22 integration incl. 6 `EngagementFlowTests` E2E: multi-user counts + double-like/unlike no-ops, batch summaries + 422 guardrails, private-category 403-for-intruder vs owner likes, 404s for missing/garbage targets, comment+thread delete cascades via consumers with counter checks, user_stats karma=2 and unknown-user 404).

**Phase 6 — Messaging backbone: code-complete + fully verified (2026-07-05, branch `11-feat-phase-6---messaging`).** Unlike Phases 1–4 this is mostly **shared infrastructure in `Forum.Infrastructure/Messaging/`** + thin per-module wiring (`AddModuleMessaging<TContext>("<module>", m => m.Consume<TEvent>()…)` in each module installer) — the previously dead consumers now run for real: outbox row → RabbitMQ → handler.
- **Relay:** generic `OutboxRelayService<TContext>` (BackgroundService, one per module) claims `outbox_messages` rows with **`FOR UPDATE SKIP LOCKED`** (polling-publisher: replicas relay disjoint batches, a crashed claim dies with its tx — deliberately NOT Files' advisory lock, which is for singleton jobs) and publishes to a **durable topic exchange per module** (`identity`/`content`/`files`/`engagement`), routing key = event short type name, **publisher confirms ON** → `ProcessedOnUtc` stamped only after broker confirm (at-least-once); publish failure → `Error` column + exponential backoff (cap `Messaging:MaxPublishBackoffMilliseconds`).
- **Consumer host:** generic `IntegrationEventConsumerService<TContext>` per consuming module declares `<module>.events` + `.retry` (TTL `Messaging:RetryDelayMilliseconds`, dead-letters back to work) + `.poison` (parking) chained via the default exchange, and binds ONLY the (exchange, routing-key) pairs registered via `.Consume<TEvent>()` — the source exchange is **derived from the contract's namespace** (typo-proof, no dead bindings). Deliveries deserialize with the shared **`IntegrationEventJson`** (the 4 module-local `UlidJsonConverter` copies were consolidated into `Forum.Infrastructure`) and dispatch through the **unchanged `IEventBus`/`InMemoryEventBus`** (now scoped, was singleton) via cached `MakeGenericMethod` — handler logic untouched. Consumers wired: Files+Engagement ← `ThreadDeleted`/`CommentDeleted`, Content ← `UserBlocked` (still log-only).
- **Idempotency:** per-module **`inbox_messages`** (migrations `AddMessagingInbox` ×4; also add `outbox_messages.correlation_id` — a deliberate step beyond "inbox only": the plan's request→outbox→consumer correlation is impossible without it). The host inserts the EventId row FIRST inside a transaction on the module's scoped DbContext; handlers share that scope/context so their effects **commit atomically with the inbox row** — a duplicate delivery hits the PK, rolls back, acks. Malformed payload / unknown routing key → poison immediately; handler failure → retry queue, after `Messaging:MaxDeliveryAttempts` total attempts (counted from the `x-death` header) → poison. (Retry-exhaustion path is unit-logic only — no deterministic handler-failure injection exists in the integration host; malformed-poison and dedupe are integration-tested.)
- **Correlation:** OutboxWriters (now take `ICorrelationContext`) stamp `correlation_id` → relay sets `BasicProperties.CorrelationId` → consumer restores it into the scope's `ICorrelationContext` (chained outbox writes carry it on) + a `BeginScope` log property; E2E test asserts the DB-level chain against the response's `X-Correlation-ID`.
- **Readiness:** `HealthCheckExtensions` TODO filled with hand-rolled `PostgresHealthCheck` (SELECT 1) + `RabbitMqHealthCheck` (same shared lazy connection as the hosts), tagged `ready` — no `AspNetCore.HealthChecks.*` packages (avoids CPM/Npgsql version juggling). `RabbitMqConnection` improved: disposes dead connections before reconnect, `AutomaticRecoveryEnabled=false` (the hosts own reconnection; client auto-recovery beside it would race and leak duplicate consumers).
- **Authz cache recompute stays synchronous in-request** (deliberate: it works and is security-sensitive — async would open a revocation window for zero gain). Identity registers relay-only messaging; its `RoleAssigned`/`AclEntryChanged`/`UserRegistered` rows still relay to the `identity` exchange for future consumers. `users.avatar_file_id`/`categories.icon_file_id` sync from `FileCommitted` remains unbuilt (no handler exists yet; the bus now makes adding it trivial).
- **Tests:** `ForumApiFactory` boots a RabbitMQ container (`rabbitmq:4-management`, same image as compose.yaml; `Testcontainers.RabbitMq 4.1.0` in CPM) alongside Postgres/MinIO with fast cadence (poll 200 ms, retry 250 ms, 3 attempts). New `MessagingFlowTests` (full-path E2E: delete thread → relay → both consumers' real effects + outbox stamped processed with correlation id + one inbox row per module; duplicate delivery = no-op with exactly 1 inbox row and empty poison queues; malformed message → parked in both poison queues while later traffic still flows) + `ReadinessTests` (own fixed-broker-port factory subclass: `/health/ready` 200 → stop broker → 503 → restart → 200; `/health/live` untouched). **FilesFlowTests/EngagementFlowTests no longer invoke handlers directly** — the Phases 3–4 placeholder is gone; they poll the real pipeline via the new `TestWait.UntilAsync`.
- **Verified green (everything):** `dotnet build` · `dotnet format --verify-no-changes` · `dotnet test` = **138 tests** (24 Engagement, 40 Files, 26 Content, 20 Identity, 2 arch, 26 integration — all E2E suites now run against the live pipeline, 0 skipped).

**Phase 7 — WebSocket real-time: code-complete + fully verified (2026-07-05, branch `13-feat-phase-7---websocket`).** Lives in **`Forum.Api/Realtime/`** (Bootstrap — the hub is host wiring, not a module; it consumes Content/Engagement **Contracts only**). Endpoints `POST /api/realtime/ticket` + `GET /api/realtime/ws` (both in OpenAPI, verified live).
- **Hub = lean bus consumer (ADR 0010), deliberately NOT `AddModuleMessaging`:** `RealtimeChangeFeedService` binds a **server-named exclusive auto-delete queue per replica** to the `content`/`engagement` exchanges (8 routing keys from `RealtimeEventMap`, reusing `MessagingTopology`/`IntegrationEventJson`/shared `IRabbitMqConnection`) — **fan-out-to-every-replica**, the opposite of the module hosts' competing-consumers `<module>.events` queue. **No DB inbox/retry/poison, autoAck** — a push has no persistent effect, duplicates/losses self-heal via the client's reconnect-resync; a shared inbox would even re-introduce cross-replica dedupe and silently break the fan-out.
- **Events wired:** Thread Created/Updated/Deleted, Comment Created/**Updated** (new `CommentUpdatedIntegrationEvent` — UpdateComment never published one; added to Content Contracts + outbox write), Comment Deleted, ReactionAdded/Removed. Skipped deliberately: `CategoryCreated`, `FileCommitted`, all Identity events (no live view consumes them yet). **CategoryId added at the source** (free — the publishing handlers already hold the category): ThreadUpdated/ThreadDeleted/CommentCreated/CommentDeleted += `CategoryId` (PinThread/ChangeThreadCategory stamp it too; a move carries the **target** category); Reaction events += `CategoryId`+`ThreadId` via `ReactionTarget`+reader SQL (`t.id`). **Envelope `{type, entity, id, parentId?, categoryId}` — ADR's `version` dropped** (no aggregate has a concurrency token; a fake counter would imply ordering the at-least-once bus can't promise — freshness comes from the re-fetch). `parentId` = containing thread for comments/reactions; for `reaction`, `id` = the reacted target (client knows its type from its own view).
- **Handshake auth = short-lived single-use ticket** (`RealtimeTicketService`; browser WS can't send Authorization, and the in-memory access token must not land in URLs/logs): the ticket is an HS256 JWT (same signing key, **audience `<aud>:realtime`**, TTL `Realtime:TicketTtlSeconds` = 30 s, jti, zero clock skew) minted by the bearer-authed ticket endpoint and redeemed by `/ws?ticket=` — self-contained, so **any replica validates it without sticky sessions**; single-use via per-replica redeemed-jti cache (cross-replica replay bounded by the 30 s TTL; shared cache = Phase 10 hardening option if ever needed). Access-token-as-ticket fails on audience.
- **Protocol:** client `{"action":"subscribe|unsubscribe","view":"category|thread|user","id":"<ulid>"}`; server acks `subscribed`/`unsubscribed` (tests sync on them), `error` reasons `unknown-view`/`unknown-action`/`malformed-message`/`forbidden-view` (**user view is self-only**)/`too-many-subscriptions` (`Realtime:MaxSubscriptionsPerConnection` = 64); notifications are distinguished by having `entity`. Sockets are always authenticated; **subscribe is always accepted — authorization happens on every push**: `RealtimeNotificationDispatcher` resolves the category once per event via the **new Content Contracts surface `IContentVisibility.GetCategoryAccessAsync`** (`CategoryAccessReader`; null/deleted → nothing pushed) and, for private categories, checks each subscriber owner-or-`moderate` through `IPermissionService` in a fresh scope — a revocation gates the very next push. Reaction events also route to the **actor's own user view** (multi-device like-state sync).
- **Tests:** new **`Forum.Api.Tests`** project (20 unit: event-map envelopes/drops, subscription parse/match/cap, ticket lifecycle incl. expired/replayed/garbage/access-token-as-ticket) + `RealtimeFlowTests` (4 E2E over `TestServer.CreateWebSocketClient`): full REST→outbox→relay→exchange→hub→socket push with envelope assertions (incl. **no `version` field**) and the forbidden foreign-user-view error; **two-replica fan-out** — a second `RealtimeChangeFeedService` on its own broker connection + recording sink, one REST-driven event reaches BOTH the host replica's socket and the second replica's feed (probe-until-bound makes it deterministic); private category: intruder never receives while a moderator does, then **role revoked mid-connection** stops the next private push (sentinel-ordered, relay orders by ULID id); reconnect: old ticket rejected (single-use), garbage ticket rejected, fresh ticket + resubscribe receives only post-reconnect events (gap event provably drained first — no server-side replay). `InternalsVisibleTo` on Forum.Api for Api.Tests/IntegrationTests; `System.IdentityModel.Tokens.Jwt` now an explicit Api package ref (already CPM-pinned).
- **Verified green (everything):** `dotnet build` · `dotnet format --verify-no-changes` · `dotnet test` = **162 tests** (20 Api, 24 Engagement, 40 Files, 26 Content, 20 Identity, 2 arch, 30 integration, 0 skipped).

**Phase 8 — Frontend (React SPA): code-complete + verified (2026-07-06, branch `15-feat-phase-8---frontend`).** Lives in **`frontend/`** — Next.js 15 (App Router) + TypeScript + npm, used strictly as an app shell/router/build tool: **pure CSR, no Next server ever talks to the .NET API** (React Query + browser fetch only). Design mockups + tokens moved to `frontend/design-reference/` (reference-only); tokens copied into `src/styles/tokens/` (fonts self-hosted via next/font feeding `--font-*-base` vars).
- **API layer** (`src/lib/api/`): one `apiFetch` client (base URL from `NEXT_PUBLIC_API_URL`, default `http://localhost:5099`) + `problem.ts` RFC7807 mapper handling the documented quirks (empty-body 429 → "slow down" toast, envelope-less 401/403, bespoke `{error}` 400); hand-authored types verified against backend DTO records; query-key factory shared with WS invalidation.
- **Auth**: access token in module memory only (`lib/auth/token-store.ts`) with **single-flight silent refresh** shared by the 401-retry interceptor and AuthProvider; refresh cookie untouched by JS; proactive refresh ~60 s before expiry; current user decoded from JWT claims (`sub`/`name`/`email`/`role`); register chains login.
- **Realtime** (`lib/realtime/`): framework-agnostic `RealtimeSocketManager` (DI'd ticket mint + socket factory, unit-tested) — fresh single-use ticket per attempt, exponential backoff, ref-counted subscriptions replayed on every reconnect, **connect event → invalidate all active queries** (resync); notifications map to targeted `invalidateQueries` in `invalidation.ts` — EXCEPT `thread created`, which feeds the **LIVE banner** (never silent feed reorder). TopNav shows LIVE/RECONNECTING/OFFLINE pill; LIVE ACTIVITY panel + bell fed from a rolling notification log.
- **KEY DEVIATION — home feed is a client-side k-way merge** (`lib/feed/feed-merge.ts`, unit-tested): `GET /api/content/threads` REQUIRES `categoryId` (no global feed endpoint), so "All threads" merges per-category keyset feeds newest-first, pinned split out on ingest, emitting only while every refillable source has a buffered head (cursors stay opaque). Feed/search cards render **no excerpt** (not in `ThreadFeedItemResponse`) and **no comment count** (hard-coded 0 server-side); like counts hydrate via the Engagement batch endpoint.
- **Markdown**: react-markdown + rehype-sanitize (raw HTML never parsed; sanitizer schema extended ONLY with the `image:`/`video:` pseudo-protocols). **Inline-media convention** documented in `lib/markdown/media-convention.ts`: `![caption](image:<fileId>)` + `@video(<ref>)` (remark plugin, AST-based so code fences are safe; GFM autolink handled via paragraph text-content matching) → resolved through `GET /api/files/{id}` at render time. Editor (WRITE/PREVIEW) uploads images via the real initiate→presigned PUT (XHR progress)→commit flow and inserts the token at the cursor; composer attaches READY files to the created thread/comment; avatar = attach with replace semantics on profile.
- **Pages**: `/` (3-col, merged feed + pinned + banner + mock POPULAR TAGS w/ SOON badge), `/c/[slug]` (most-popular callout computed from loaded batch counts; pin toggle shown to global mods via role claim — heuristic only, 403 handled), `/t/[id]` (author stats card, TOC from headings, related-by-first-tag via search, non-inline attachments rail, live-deleted banner; comment tree renders the flat path-ordered list directly, reply disabled at depth 5, `[deleted]` tombstones, realtime NEW glow excluding own comments), `/search` (debounced, URL-synced), `/u/[userId]` (real stats; activity = SOON empty state), `/auth` (409 → inline field error, 401 → banner), `/social` (**UI-only mock with persistent PREVIEW banner**, zero fetches), 404/error boundary per design. Compose = global modal (edit mode: tags fixed + category locked — PUT accepts only title/body).
- **Verified green:** `npm run typecheck` · `npm run lint` · `npm test` = **40 Vitest tests** (RFC7807 quirks, 401-refresh single-flight/retry-once, feed merge incl. blocking + tie-break, socket manager ticket/resubscribe/refcount/offline-degrade, invalidation mapping, markdown sanitization + media convention incl. fence safety, tag slug validation, depth cap) · `npm run build`. **Live smoke against the real stack** (compose infra + migrated API): register→login→category→thread+tags→comment→like→batch→ticket all 2xx; all routes served by `next dev`; feed-without-categoryId 422 confirmed (merge premise).
**Phase 9a — Backend observability finalization: code-complete + fully verified (2026-07-10, branch `17-feat-phase-9---backend-observability`).** Spec: `docs/architecture/PHASE-9-10-ENTERPRISE-PLAN.md` §9a; **the exported names/keys 10c must use live in `docs/architecture/OBSERVABILITY-CONTRACT.md`** (verified by tests, corrects the plan's `"TraceId":"…"` derived-field regex → compact JSON actually emits `"@tr"`).
- **`ForumMetrics`** (`Forum.Common/Telemetry`, BCL Meter `Forum`, semantic methods so tag sets stay closed in one place; singleton in `AddForumInfrastructure`, exported via `AddMeter`): plan's 11 instruments + `forum.api.rejections` (status+errorType), `forum.errors.unhandled` (closed category set database/timeout/other), `forum.hosted_service.tick_age` gauge (per-loop heartbeat, boot-tick so a dead-from-birth loop is visible; `/health/ready` only proves Postgres/RabbitMQ). Wired: Login (success/invalid_credentials/**blocked** — IsActive check split from Verify, response stays non-revealing), CreateThread/CreateComment, Add/RemoveReaction (real toggles only), relay (publish+lag histogram with seconds-scale bucket advice/failures), consumer host (ok/retry/poison/duplicate), WS hub (connections/subscriptions via count-delta + close releases remainder/pushes actually sent).
- **Error handling (the real bugs, all regression-tested):** CONFIRMED the unhandled-exception log line lost `CorrelationId` (LogContext scope pops during unwind) AND the 500 response lost the `X-Correlation-ID` header (`UseExceptionHandler` → `Response.Clear()`). Fix: `GlobalExceptionHandler` reads `ICorrelationContext` explicitly → header re-stamp + explicit log property + `forum.errors.unhandled`; ONE `CustomizeProblemDetails` hook stamps `correlationId` into EVERY ProblemDetails body (`Results.Problem` routes through `IProblemDetailsService` since .NET 8 — verified by test) and does rejection accounting (Debug log + counter; deliberately not Error). Client-disconnect OCE → 499 + Information, excluded from error metric. `RouteHandlerOptions.ThrowOnBadRequest=true` (found live: outside Development malformed JSON was a bare body-less 400) → `BadHttpRequestException` → RFC7807 400 `errorType=BadRequest`.
- **Tracing/logs:** `.AddNpgsql()` wired + health/metrics span filter + `deployment.environment` resource attr + `Otlp:Endpoint` honored. **EF Core instrumentation deliberately NOT added** (Npgsql's ActivitySource already spans every command incl. raw-ADO reads; EF's would double-span writes). `appsettings.Production.json` → **`RenderedCompactJsonFormatter`** stdout (Alloy ships; Loki sink package dropped from csproj+CPM); `UseSerilogRequestLogging` GetLevel quiets `/health|/metrics` to Verbose; Serilog overrides add `Microsoft.EntityFrameworkCore.Database.Command` + `Npgsql.Command` = Warning (EF/Npgsql log every command at Information).
- **Hardening found by (B) audit:** all four BackgroundServices' catch filters flipped from `is not OperationCanceledException` to token-based — a client-internal OCE (broker timeout) previously ended relay/consumer/sweeper/realtime-feed loops **silently forever**.
- **G6/G5/G19/G17:** `UseForwardedHeaders` first in pipeline, trust from `ForwardedHeaders:KnownNetworks` config (`KnownIPNetworks` — .NET 10 obsoleted `KnownNetworks`), default = loopback-only so dev/compose never trusts spoofed headers; `StorageOptions.PublicEndpoint(+PublicUseSsl)` → second MinIO client used ONLY for presign (interface unchanged, presign is offline computation); Production boot without `Jwt:SigningKey` throws at registration; `HostOptions.ShutdownTimeout=25s`.
- **Tests 188 total (0 skipped): 29 Api** (+9: exception pipeline E2E over TestServer incl. correlation-in-log/header/body regression + rejection-counter + malformed-JSON, cancellation carve-out units, forwarded-headers trust matrix, **SerilogJsonShapeTests pinning `@t/@m/@l/@x/@tr/@sp/CorrelationId`**), **37 integration** (new: `ObservabilityFlowTests` — all `forum_*` series on live `/metrics` after real traffic incl. delete→relay→consumer, 404 echoes correlation id in header+body; `MinioPresignTests` public/internal host binding; `ProductionJwtFailFastTests`), 24 Engagement/40 Files/26 Content/30 Identity/2 arch. `dotnet build` · `format --verify-no-changes` · full `dotnet test` green; live Production-mode smoke verified config-loaded JSON formatter, quieted probe logs, `forum_errors_unhandled_total{category="database"}`, and response↔log↔trace id correlation on a real 500.

**Phase 9b — Deterministic seed: code-complete + fully verified (2026-07-11, branch `17-feat-phase-9---backend-observability`).** Spec: `docs/architecture/PHASE-9-10-ENTERPRISE-PLAN.md` §9b (its top-of-block "IMPLEMENTED" callout has the locked numbers + wiring corrections). Two named profiles, ONE seeder implementation per module, branch on `config.Profile` for counts only.
- **Shared seeding** in `Forum.Infrastructure/Seeding/`: `SeedProfile{Development,Benchmark}`, `SeedConfig(Profile,AllowTruncate,Verbose)`, `SeedPlan` (the single source of truth for counts + role/category conventions), `SeedStreams`, `SeedTime` (wall-clock-free deterministic timestamps), `SeedUlids` (`Ulid.NewUlid(SeedTime.At(stream,i), SHA256(seed:stream:i)[..10])` — pure fn of (stream,index), reproducible; the plan's `NewUlid(baseTime+i*offset)` was WRONG, that overload uses a crypto RNG), `SeedDistribution.ZipfIndex`, `IModuleSeeder`. **Seeding = its own CLI arg**, mirroring `migrate`: `Program.cs` `seed` branch → `SeedRunner.RunSeedAsync` (Startup, parallel to `MigrationRunner`) resolving `IModuleSeeder`s in order — NOT registered as `IStartupTask`s (would fire every boot).
- **Per-module seeders** (`IdentitySeeder`→`ContentSeeder`→`EngagementSeeder`, in each `Infrastructure/Seeding/`, registered in the installer): write directly through the module DbContext in 1000-row batches with `ChangeTracker.Clear()`, via new internal domain `Seed(…)` factories (`User/Category/Thread/Comment/Tag`) that set an explicit id + audit and **raise no events**; plain `SaveChangesAsync` (not the dispatch variant) → **zero outbox rows** (verified). ONE precomputed Argon2id hash per profile (random salt per run → password_hash not byte-reproducible, but login verifies; determinism is about ids/keysets). `search_tsv`/`reaction_counts` triggers fire on insert → free + verified consistent. Comment paths built exactly like `Comment.CreateReply` (`parent.Path + "." + id`, depth-capped).
- **AuditInterceptor** now skips stamping when `CreatedOnUtc` is already set (freshly-constructed aggregates are always `default`, so request-path inserts are unaffected) — this is what lets seeded deterministic timestamps survive. Users' `created_by` = null (matches anonymous self-registration); content `created_by` = owner.
- **Authz:** every user gets `user`; staff also `admin`/`moderator` (fixed role ids `…001/002/003`); private-category "members" get a **`moderate` ACL at category scope** (bit 6 = 64) — the code's private gate is owner-or-moderate, so that is the grant that opens it. IdentitySeeder owns all `forum_authz` writes (its schema) using deterministically-reconstructed category ids (no Content ref); role grants + ACLs are one bulk `unnest` INSERT each, then one bulk `recompute_user_perms`.
- **Files seeding deliberately omitted** (plan listed 5–10 Dev PNGs): a committed `files` row pointing at a never-uploaded MinIO object would break presigned GET, and the seed CLI needs only Postgres. Avatars/icons stay null (SPA handles it); Benchmark seeds 0 files anyway (uploads = a 9c k6 scenario).
- **Profiles (LOCKED):** Development = 5 users (admin/mod/alice/bob/charlie, pw `Dev#Password1`), 2 categories (general public + private-club private), 4 tags, 10 threads, 10 comments, ~5 reactions (~<1 s, ~1 MB). Benchmark = 800 users (2 admin/10 mod/20 blocked, pw `Bench#Password1`), 12 categories (4 private × 25 member ACLs), 60 tags, 1600 threads (1% pinned/deleted), 9000 comments (depth 0–4, path ≤134), 15000 reactions (Zipf 75/25 thread/comment). **Measured `pg_database_size` = 24 MB, seed ≈ 13 s** (plan's 200–400 MB estimate was ~15× high; numbers kept — far under the 1 GiB Postgres limit).
- **DB isolation:** `compose.yaml` already defaults `${POSTGRES_DB:-forum_net}` (unchanged). `scripts/seed-test-data.sh` (rewritten from the TODO stub) + `make seed` → `forum_net`; `make seed ARGS=--benchmark` → `CREATE DATABASE forum_net_bench` idempotently (`lib.sh ensure_database`) + `--force`, both datasets coexist; `ARGS=--cluster` applies the k8s Job (`k8s/backend/seed-job.yaml` dev / `seed-job-benchmark.yaml` `--force`). Guard: `seed` (no `--force`) aborts non-zero on a non-empty DB.
- **Verified green (everything):** `dotnet build` · `format --verify-no-changes` · full `dotnet test` = **200 tests** (40 Api incl. 11 `SeedGeneratorTests`, 38 integration incl. `SeedFlowTests` = Development e2e: counts + roles/ACL + FTS + zero reaction_counts drift + real admin login on reused hash + determinism across two fresh runs + idempotency-guard abort; 24 Engagement/40 Files/26 Content/30 Identity/2 arch). Manual: Benchmark `make seed ARGS=--benchmark` measured 24 MB, deterministic across `--force` re-runs (md5 of thread/comment/user id sets identical). Phase 9b work uncommitted — review then commit.

**Phase 10b — Kubernetes core (infra manifests, security, networking): code-complete + verified live (2026-07-11, branch `21-feat-phase-10b---kubernetes`).** Spec: `docs/architecture/PHASE-9-10-ENTERPRISE-PLAN.md` §10b — **its top-of-block IMPLEMENTED callout records every deviation**; runbook `docs/runbooks/wsl-minikube-setup.md` fully rewritten (§4 = the Windows-access architecture, a first-class deliverable of this phase).
- **Cluster shape:** minikube profile `forum`, `--cni=calico` (REQUIRED — default CNI silently ignores NetworkPolicies; CNI can't be swapped live, stale clusters need `mk-down --delete`), addons ingress+metrics-server; namespace `forum-dotnet` labeled **PSS `restricted`** (enforce+warn+audit — zero warnings on deploy). `k8s/rbac.yaml`: 5 tokenless ServiceAccounts (`automountServiceAccountToken: false`, deliberately NO Roles — nothing talks to the k8s API).
- **New/rewritten manifests:** postgres (uid 999, ro-rootfs + run/tmp emptyDirs, PGDATA subdir, 4Gi PVC, args-tuned, `max_connections=100` kept — pool math `3×30+overhead ≤ 100` lives in the statefulset header + CONNECTION_STRING gains `Maximum Pool Size=30`); rabbitmq 4 (uid 999 verified, `RABBITMQ_DEFAULT_USER=forum` secret — G14 guest-loopback; management+prometheus plugins, per-object metrics); minio (pinned `RELEASE.2025-09-07T16-13-09Z`, CORS env for the SPA origin, dedicated ingress `minio.forum.local` with scoped `proxy-body-size: 10m` — presign via ingress host, NOT NodePort) + `minio-create-bucket` Job (mc pinned, restricted securityContext, own netpol entry via `app: minio-setup`); frontend (ro-rootfs + `.next/cache` emptyDir, **`runAsUser: 1000` pinned — node image USER is the non-numeric string `node`**, kubelet can't verify runAsNonRoot otherwise); backend (**seccompProfile added, `runAsUser` dropped; root `Dockerfile` changed `USER app`→`USER 1654`** — same trap as frontend, 10a's `USER app` had replaced the base image's numeric Config.User with a string and every pod died CreateContainerConfigError; found live), maxSurge 1/maxUnavailable 0, tGPS 40 + native preStop sleep 5, `replicas` removed (HPA owns scale), envFrom all 4 secrets; hpa gains the plan's behavior block; Jobs get `app: backend` labels (postgres netpol), restricted contexts, `activeDeadlineSeconds`, and **`backend-secrets` — the 9a Production Jwt fail-fast fires at registration even for `migrate`**.
- **Networking:** ingress + TLS (mkcert cert `forum.local`/`minio.forum.local`/`grafana.forum.local` via `scripts/mkcert-tls.sh` → gitignored `k8s/ingress/tls/` → `forum-tls` secret; `ssl-redirect` on, WS `proxy-read/send-timeout 3600`); frontend image now bakes **`https://forum.local`** (build-images.sh default flipped — scheme change = image rebuild, not manifest edit). NetworkPolicies: default-deny-ingress + 5 allow files (backend←ingress-nginx/monitoring:8080, postgres←`app=backend`+monitoring:5432, rabbitmq←backend:5672+monitoring:15692 (mgmt NOWHERE — port-forward only), minio←backend/minio-setup/ingress-nginx/monitoring:9000, frontend←ingress-nginx:3000); `monitoring` ns selectors are inert until 10c creates it. **Egress deliberately not denied** (recorded decision in default-deny.yaml). `APPLY_NETWORK_POLICIES=true` is now the default.
- **deploy.sh rewritten** (order: images → ns/PSS+rbac → secrets ×4 generate-if-missing (existing in-cluster secrets never overwritten; rabbit password/JWT key random unless in .env) + forum-tls from files → infra+backend-configmap (Job consumes it — apply-order bug found live) → rollouts → bucket Job → migrate Job → optional `--seed`/`--seed-benchmark` → backend → frontend → ingress ×2 → netpols). **Image pinning = apply-time sed substitution (`apply_with_tag`) of the manifests' `:local` placeholder to the git-SHA tag** — deviation from the sketched `kubectl set image` (one rollout instead of two, Jobs on the same tag, history still SHA-addressed); `reset-db.sh`/`seed-test-data.sh --cluster` pin Jobs to the LIVE backend image instead. configmap: Otlp→`tempo.monitoring...:4317`, `Storage__PublicEndpoint=minio.forum.local`+`PublicUseSsl=true`, CORS `https://forum.local`, `ForwardedHeaders__KnownNetworks__0=10.244.0.0/16` (verified live = calico pod CIDR).
- **Windows access (empirically settled):** `minikube ip` is NOT reachable from Windows (TCP+ICMP fail — docker bridge inside the WSL VM); WSL2 localhost-forwarding IS (verified; `.wslconfig` sets it true; test with `curl.exe`, NOT Invoke-WebRequest — false negatives). Everything Windows-facing rides **`scripts/dev-tunnels.sh` / `make tunnels`**: table-driven port-forwards (extensible array — 10c adds Grafana/Prometheus lines), local = remote+10000 (never compose's ports; **Postgres = 15432** for DataGrip — 5432 would silently hit the compose DB), auto-reconnect loops, one Ctrl+C kills all (verified), prints all credentials from cluster secrets. Ingress rides real 443/80 after a one-time `ip_unprivileged_port_start=80` sysctl (8443 fallback + printed fix otherwise); Windows hosts file → **127.0.0.1** (never minikube ip); mkcert CA must ALSO go into the WINDOWS cert store (walkthrough in runbook §4b). Port-forward is architecturally UNAFFECTED by netpol/calico/PSS (API-server path) — hardening never locks the admin out. **Swagger UI is Development-only → 404 in the cluster by design** (noted in tunnels output).
- **Verified live (full DoD, one pass):** all pods Ready under enforce=restricted with 0 PSS warnings; migrate+seed+bucket Jobs Complete (dev seed: 5 users/2 cats/10 threads); full journey through real ingress+TLS from WSL (`curl --resolve`): SPA HTML, 308 http→https, login (refresh cookie **HttpOnly+Secure**), register 201, thread/comment/like, **presign flow G5/G20 end-to-end** (uploadUrl host `https://minio.forum.local`, PUT 200 via ingress, commit probes 1×1 PNG, GET roundtrip byte-identical), realtime ticket + **WS 101 through nginx**, `/metrics` NOT ingress-exposed (404), 422s carry correlationId+traceId; netpol probe FAILS as required (busybox+PSS-compliant overrides → `nc postgres:5432` times out) while `app=backend`-labeled control CONNECTS; `rollout restart`+`rollout undo` clean with API 200 after; HPA live (cpu 13%/70%); **from Windows** (curl.exe/PowerShell): backend Healthy via :18080, SPA via ingress tunnel with SNI, RabbitMQ mgmt 200, MinIO console 200, tcp/15432 True + real SQL through the tunnel returning the cluster-registered smoke user; tunnel cleanup leaves zero listeners/processes.
- **Prereq flagged for 10c/benchmarks:** this machine's `.wslconfig` grants 10GB → verified at `MINIKUBE_MEMORY=8192`; bump `.wslconfig` to 12GB + `.env` to 10240 (plan §1) before the monitoring stack + measured runs. `.env`/`.env.example` updated (6 CPUs, netpols on, IMAGE_TAG unpinned, RABBITMQ_*/JWT_SIGNING_KEY knobs); preflight now checks RAM headroom, localhostForwarding, mkcert, helm. Phase 10b work uncommitted — review then commit.

**Phase 10c — Monitoring stack: code-complete + verified live (2026-07-12, branch `23-feat-phase-10c---monitoring`).** Spec: `docs/architecture/PHASE-9-10-ENTERPRISE-PLAN.md` §10c — **its top-of-block IMPLEMENTED callout records every deviation**; metric/log-key ground truth stays `docs/architecture/OBSERVABILITY-CONTRACT.md`.
- **Helm (monitoring-only), pinned 2026-07-12:** kube-prometheus-stack **87.15.1** (release `monitoring`) + loki **7.0.0** (SingleBinary/filesystem/24h + ingestion caps; `persistence:false` needs an explicit emptyDir at /var/loki — the chart mounts nothing and crashloops on the ro-rootfs; SimpleScalable replicas 3/3/3 zeroed explicitly) + alloy **1.10.1** (DaemonSet, tails pods via kubelet API — no hostPath; `stabilityLevel: public-preview` for `loki.source.kubernetes`; ships lines whole, `| json` at query time) + tempo **2.2.3 from grafana-community** (grafana/tempo chart deprecated — repo migration; HTTP API port **3200**, the plan's 3100 is Loki's) + postgres-exporter **8.1.1** (`fullnameOverride: postgres-exporter`; creds = app-ns `postgres-credentials` mirrored into `monitoring` by monitoring-up.sh). Versions in `lib.sh`/`.env.example`; values in `k8s/monitoring/values-*.yaml`, each header names its tested chart version.
- **PSS correction (live-confirmed):** `monitoring` ns = **enforce `privileged`** + warn/audit `baseline` — the plan's "baseline" enforce would REJECT node-exporter (hostNetwork/hostPID/hostPath violate baseline; the install's PSS warning proved it). App ns stays `restricted`.
- **8 dashboards** (plan's 7 + **`errors-and-problems`**: Loki recent-errors table (`| json | _l=~"Error|Fatal"`), unhandled-by-category, 401/403 attack-shaped vs 422 frontend-bug-shaped rejection split, failing-routes table, **`forum_hosted_service_tick_age_seconds` per-loop panel** — the dead-loop detector `/health/ready` can't see, poison trend). Plain JSON in `k8s/monitoring/grafana-dashboards/` (Grafana folder "Forum", uids `forum-*`), wrapped into labeled ConfigMaps by monitoring-up.sh; **`QUERIES.md` generated by `extract-queries.py`** = greppable query index for 9c's bench-run.sh. Every name live-verified: **.NET runtime = `dotnet_*`** (the plan's `process_runtime_dotnet_*` don't exist); **minikube cAdvisor exports pod-slice series only (no `container` label)** → queries/alerts use `container=""`; MinIO bucket usage lives on a SECOND scrape endpoint `/minio/v2/metrics/bucket`, TTFB = `minio_s3_requests_ttfb_seconds_distribution` (no `_bucket` suffix); `kubelet_volume_stats_*` doesn't exist on hostpath PVs (PVC panel/alert kept, documented live-empty).
- **15 alerts** (`prometheus-rules.yaml`, evaluate-only — Alertmanager stays off): the plan's 13 (PodMemoryNearLimit reworked pod-level) + `BackgroundLoopStale` + `UnhandledErrorsPresent` from the contract. `PoisonQueueNonEmpty` proven end-to-end: hand-published malformed message → parked in files+engagement `.poison` → pending → **firing**; queues purged after. **Backend ServiceMonitor needs `honorLabels: true`** (found live): the operator's `service=<Service>` target label collides with the Meter's `service` tag → loop names became `exported_service` and BackgroundLoopStale false-fired on the sweeper until the fix.
- **Correlation verified for real:** Loki `CorrelationId="…"` search → line's `@tr` == response traceId → Tempo trace (HTTP + `postgresql` spans) → logs. **Exemplars required TWO backend fixes** (contract §4): `.SetExemplarFilter(ExemplarFilterType.TraceBased)` in `ObservabilityExtensions` (SDK default is AlwaysOff — the plan's "TraceBased is default" is stale) AND the **OTel package family bumped 1.10 → 1.16** in CPM (OpenMetrics exemplar exposition only exists in Prometheus exporter 1.16.0-beta.1+, which pins core ≥1.16; bump also cleared the NU1902 advisories — build now 0 warnings; 40 Api.Tests + ObservabilityFlowTests re-run green on 1.16). Image rebuilt+rolled, exemplar→Tempo hop verified live. Grafana: Loki derived field on `"@tr":"(\w+)"`, Tempo `tracesToLogsV2`, datasource uids `prometheus`/`loki`/`tempo`.
- **Scripts/targets:** `monitoring-up.sh` (idempotent: repos → ns+PSS → secret mirrors incl. forum-tls copy → helm ×5 pinned → monitors/rules/ingress → dashboard ConfigMaps → rollout waits), `monitoring-down.sh` (uninstall ×5 + ns delete; operator CRDs kept deliberately), `monitoring-check.sh` (**make mon-check**: every scrape target UP + Loki ingesting + Tempo ready — passed clean), Makefile `mon-up/mon-down/mon-check`, dev-tunnels += grafana :13001 / prometheus :19090 against the **verified** Service names `monitoring-grafana`/`monitoring-kube-prometheus-prometheus` (skipped with a hint when the ns is absent). Grafana ingress `grafana.forum.local` (10b cert SAN; admin/admin). Backend deployment's dead `prometheus.io/*` annotations deleted.
- **Verified live (still on MINIKUBE_MEMORY=8192 — this box's .wslconfig grants 10GB):** all Prometheus targets UP (backend/rabbitmq/minio×2/postgres-exporter/kubelet+cAdvisor/node-exporter/kube-state-metrics); 86 dashboard queries → 0 errors, 1 documented-empty (PVC/minikube); monitoring stack actual ≈ **944Mi RSS / ~54m CPU** (requests 1730Mi ≈ §1's 1680Mi + Grafana sidecars), node 39% mem with both stacks up. `dotnet build` green (24 pre-existing NU1902 audit warnings only). Phase 10c work uncommitted — review then commit.

**Phase 10d — Performance & caching: code-complete + verified live (2026-07-15, ran AHEAD of 9c off a live lag investigation; measured before/after runs still owed once 9c lands).** Spec: `PHASE-9-10-ENTERPRISE-PLAN.md` §10d — **its top-of-block IMPLEMENTED callout records everything**; summary:
- **Redis verdict CONFIRMED against live data: no Redis** (all hot reads = 1–2 sub-2ms Npgsql spans in Tempo; nothing for a cache tier to win; ticket-replay stays the documented accepted exposure). Rate limiter stays per-replica (×replicas multiplier = thesis note, not a hole).
- **§10d optimizations applied + live-verified:** `Max Auto Prepare=20;Auto Prepare Min Usages=2` (deploy.sh + secret.example + patched into the LIVE secret — generate-if-missing never rewrites existing ones); ingress gzip via idempotent ConfigMap merge-patch in `setup-minikube.sh` (verified `content-encoding: gzip` through `https://forum.local`); `DOTNET_ThreadPool_MinThreads: "64"` in backend configmap (login ≈ 409ms pure Argon2id CPU on a 1-processor-visible pod; NOTE: the idle thread gauge will NOT read 64 — it's an injection floor, not pre-created threads); GC verified and deliberately left knob-less (container-aware 75% default already = the sketched setting).
- **Server-side N+1 sweep CLEAN in Tempo** (feed 2 / detail 1 / comments 2 / batch 1 / search 1 / files 1 Npgsql spans). The REAL N+1 was the SPA over HTTP: `GET /api/files` was the top API route (avatar/icon per card) + 179 single reaction GETs duplicating 77 batches. Fixed frontend-side: `lib/api/stale-times.ts` tiers (realtime-covered 5min — WS push-invalidation owns freshness while subscribed, staleTime only governs back-nav; presigned-file queries 5min hard-bounded by `Files:DownloadUrlTtlMinutes=15`; reference 60s; search 30s), `useReactionBatch` write-through to per-target keys + `covered` ReactionButtons (never self-fetch under a page batch), reconnect resync narrowed to push-covered key roots (threads/comments/reactions).
- **WS hub hardening:** `RealtimeConnection.TrySendAsync` now aborts a socket whose send/gate-wait exceeds 5s — one stalled peer (full TCP window) previously head-of-line-blocked ALL pushes on the replica (bus delivers events one at a time). Unit-tested (`RealtimeConnectionTests`, stalling fake socket). Dispatcher per-subscriber permission checks measured a non-issue — left alone.
- **Dashboards:** `overview.json` aggregate p95 + `app-red.json` per-route p95 also excluded `/api/realtime/ws` (same socket-lifetime pollution as the previously-fixed aggregate panels); QUERIES.md regenerated; both ConfigMaps redeployed. Tempo OOM'd (137, 512Mi) under scripted TraceQL search bursts — pace it or raise its limit at the memory bump.
- **Deliberately NOT done:** React-Query persister (presigned URLs expire in 15min — restoring them across reloads = broken images), IntersectionObserver auto-fetch (LOAD MORE ↓ is the design's deliberate affordance + limiter budget), CategoryCreated realtime event (rarely-changing entity, not worth bus surface), files batch endpoint (staleTime already removed the repeat traffic; add only if first-render fan-out shows up in 9c).

**Phase 9c — k6 load profiles + benchmark runbook: code-complete + verified live (2026-07-16, branch `25-feat-phase-9c---k6-load-profiles`).** Spec: plan §9c — **its top-of-block IMPLEMENTED callout records every deviation**. k6 **v2.1.0** portable binary in `~/.local/bin` (no sudo, no packages).
- **`load/k6/main.js`** (single entrypoint, `smoke.js` deleted) + `lib/api.js` (endpoint-tagged helpers) + `lib/assets.js` (real 68-byte 1×1 RGBA PNG — programmatically generated, valid CRCs, ImageProbe-parseable; the seeder's exact word bank as FTS corpus). Profiles: smoke 5 VU/60 s (p95<500), demo 0→10→40→80/7 min (p95<800 — the HPA showcase), stress 0→50→100→150/6.5 min (p95<2000 informational; runner maps k6 rc 99→0 for stress only — it documents the knee, never aborts). Parallel **`ws` scenario** (demo 20/stress 40 VU): ticket→wss→subscribe random public category; **120 s hold cycles** with a fresh single-use ticket per reconnect (crosses the G13 60 s idle boundary AND re-exercises mint→handshake→subscribe).
- **Mix** per plan (30 feed +30% cursor-follow / 20 thread-open = the SPA's real 3-call pattern / 10 search / 8 tags / 10 comment / 5 thread+tags / 12 reaction-toggle / 3 presigned upload / 2 profile), think 0.3–0.7 s. Surface corrections: profile mix = `stats`+`users/{id}/threads` (no `GET /api/identity/users/{id}` exists); comments top-level only (reply→depth-5 422 risk). setup(): ≤5 rps staggered logins of 200 ordinary bench users (`bench_user_0034+`, skips staff/blocked bands; dev-seed fallback alice/bob/charlie), category/thread pools discovered via the public API (public categories only — private 403s would pollute failure rates). Thresholds bind to `{scenario:http}` (setup's Argon2id logins stay out of p95); per-endpoint informational thresholds materialize the thesis tables; **`forum_rate_limited` `count<1` on every profile** — any 429 fails the run. k6's `hosts` option pins `forum.local`/`minio.forum.local`→minikube ip (`INGRESS_IP` from the runner; /etc/hosts here resolves them to 127.0.0.1 first and editing needs sudo); mkcert CA in system store → TLS verifies flag-free.
- **`run-load-test.sh`** rewritten: 5 s sampler (HPA current/desired/cpu% + summed backend `kubectl top`) → `load/results/samples-<stamp>.json`; k6 summary extracted from the `===K6_SUMMARY_JSON_BEGIN===` stdout block → `summary-<stamp>.json`; loud warning when demo/stress face an unraised limiter. **`bench-run.sh`** (new; `make bench ARGS="demo|stress [--repeats N] [--skip-warmup]"`): preflight (backend ready, monitoring up, seed sentinel ≥100 users) → `meta.json` (git SHA+dirty, image tag AND node imageID digest, seed counts via **scalar subqueries — UNION ALL branch order scrambles under parallel Append, found live**, limiter values, VM/WSL sizing, k6 version) → raise **Global AND Auth** PermitLimit to 1000000 (plan's "stagger instead of raising Auth" corrected: 200 logins cannot fit any stagger in the cluster's 50/min per-IP window; raise recorded in meta.json, EXIT-trap restores pre-existing overrides verbatim else falls back to configmap) → discarded smoke warm-up → **`HPA_CPU_INIT_GRACE` 300 s** → N repeats + 2 min cool-downs + per-run `kubectl get events` → Prometheus `query_range` snapshots (QUERIES.md exprs, literal `[1m]`, port-forward **:19091** — dev-tunnels owns :19090) → `stats.json` mean±stddev + fairness checklist print. Archive: `thesis/results/A/<stamp>-<profile>/`.
- **Two live cluster findings fixed (both silent HPA-staircase killers):** (1) backend probes had no `timeoutSeconds` (k8s default **1 s**) — at the 80 VU plateau the saturated pod missed the deadline, flapped unready, and the **HPA discards unready pods' CPU samples** → never scaled + brief ingress endpoint withdrawal; `k8s/backend/deployment.yaml` now sets `timeoutSeconds: 5` on both probes. (2) controller `--horizontal-pod-autoscaler-cpu-initialization-period` (~5 min) zeroes young pods' CPU and the limiter-raise rollout resets that clock — hence the bench grace wait. **Demo staircase 1→2→3 verified live post-fix** (3/3 during the 80 VU plateau).
- **Verified live** (Benchmark seed 800 users/12 cats/1600 threads/9000 comments/15000 reactions in-cluster; image `git-9d708ae-dirty`): smoke 992 reqs · 15 rps · p95 18 ms · 0% fail; demo 52.3k · 97.5 rps · p95 103 ms · 0% fail; stress 72.9k · 144 rps · p95 850 ms · p99 3.4 s · 0% fail (knee visible in write-path p99s: comment_create ≈6 s @150 VU); WS demo 80 subs/25.5k notifications/0 errors, stress 160/67.3k/0; **presigned-upload golden path green inside every profile**; k6 peak ≈600 MiB RSS, no swap pressure (12 GB WSL / 10 GiB VM). **`make bench ARGS=demo` end-to-end archive verified (`thesis/results/A/20260716-1643-demo/`, image `git-9d708ae-dirty`, clean 800/1600/9000/15000 baseline, 3 repeats): 113.5 ± 2.2 req/s · p95 34.5 ± 15.1 ms · p99 184.6 ± 187.6 ms · 0.00% failed;** full bundle present (meta.json with git SHA+digest+seed+limiter, warmup/, run-1..3 summary+samples+events, prometheus-snapshots.json 13/14 queries, stats.json); limiter auto-restored via EXIT trap. **HPA staircase — real manifest bug found + fixed live:** the archived run scaled only 1→2 and a warm-pod demo sat pinned at 1 replica at 300%+ CPU while the HPA logged `did not receive metrics for targeted pods`. Root cause: the Deployment/HPA selector is `{app: backend}` and the **migrate/seed Job pods carry the same `app: backend` label** (for the `postgres-allow` netpol) — a *Completed* Job pod (e.g. `db-seed-benchmark` from the reseed) matches the selector, has no metrics, so the HPA can't read the full pod set and conservatively refuses to scale up. Proof: deleting the Completed Job pods mid-run stepped the HPA **1→2→3 in ~40 s**. Fix applied: `ttlSecondsAfterFinished: 120` on the three Job manifests (Completed pods self-delete) + a bench-run.sh preflight sweep of terminated `app=backend` pods. Clean pool → demo scales 1→2→3 tracking the ramp, no intervention. (The earlier CPU-init-lag theory was wrong — pods were warm+Ready; the metric pool was polluted.) Ops gotcha rediscovered: mk-deploy after a namespace delete regenerates `rabbitmq-credentials` while the broker's PVC keeps the old user DB → sync via `kubectl exec rabbitmq-0 -- rabbitmqctl change_password forum <new>`; write mix grows the DB (~1.6k threads+3.4k comments per demo) — symmetric A/B, meta.json pins pre-run counts, reseed between benchmark days.

**Phase 10d — Redis addendum (2026-07-16, planned — not yet built): decision reversed for portfolio/skill-demonstration reasons; the empirical performance verdict above is unchanged and still correct.** The original "no Redis" call stands as *data*: no hot path in this app is slow enough for a cache tier to matter, and that finding is not being retracted. The reversal is a deliberate, non-performance-driven choice to add a narrowly-scoped Redis anyway, to demonstrate distributed-caching/session/rate-limiting skills. Scope (explicitly **NOT** full-API response caching): (1) category list cache, (2) app configuration cache, (3) most-popular-posts cache, (4) distributed rate limiting (replaces the per-replica `PartitionedRateLimiter`, closing the documented ×replicas multiplier from the original §10d table), (5) distributed session cache. Implementation delegated to a dedicated Fable 5 session. Detail: `docs/architecture/POST-9C-ROADMAP.md`.

**Phase 11 (plan Phase 5) — Social backend: code-complete, verified green EXCEPT dedicated E2E suite (2026-07-17, branch `27-feat-phase-11---social`).** Module `Forum.Modules.Social` (friends, peer blocks, groups, unified DM/group-chat messaging, durable notifications, privacy settings, presence) + realtime-hub generalization (**ADR 0011**) + Files social targets. Working notes/locked design: `docs/architecture/PHASE-11-SOCIAL-PROGRESS.md` (delete once E2E lands).
- **`forum_social`** — migration `InitialSocial` (EF): `friendships` (pending/accepted as text; decline/cancel/unfriend DELETE the row; EF directed-pair unique + **raw-SQL `ux_friendships_pair` LEAST/GREATEST** closes the A→B/B→A race), `social_blocks` (composite PK; peer block ≠ Identity's admin ban — block+privacy denials share ONE generic 403 per interaction so a block never reveals itself; creating one severs friendship + pending invites in-tx), `groups` (public=open join/private=invite-only; visibility affects DISCOVERY only, chat+roster stay member-only; soft-delete; owner can never leave/be kicked — transfer or delete), `group_memberships` (THE membership fact; no role column), `group_invites` (pending-only rows), `conversations` (**group chat's conversation id == group id**; direct get-or-create race-safe via partial-unique `direct_key`), `conversation_participants` (**THE single message-auth fact** — group membership writes through in-tx via `GroupMembershipWriter`; `last_read_on_utc` = own unread badge ONLY, no sender-visible receipts), `messages` (4000-char markdown, edit marker, `"[deleted]"` tombstone like Comment; keyset `ix_messages_history`), `notifications` (durable bell truth; kinds friend.request/friend.accepted/group.invite/group.invite.accepted/group.kicked; NO per-message rows), `user_privacy_settings` (absent row = defaults; friends-audience for requests normalizes to no_one), `user_presence` (heartbeat only; status computed from age at read), outbox+inbox. Migration `AddSocialViews` (raw SQL in `Infrastructure/Views/SocialViews.cs`): 9 read views incl. `conversation_list_v` (display name, last-message lateral, per-seat unread) and `group_member_v` (**is_admin resolved live from ACL, bit subqueried from the actions catalog**). Migrations folder has the generated-code `.editorconfig`; regenerate with `-o Infrastructure/Persistence/Migrations`.
- **ACL:** `PermissionScopes.Group = "group"` — zero schema changes (scope was free text), **zero new bits (8/32 used)**: group-admin = existing `moderate` bit at group scope. NEW shared port **`IAclGrantService`** (`Forum.Common/Security`, impl `Identity/Infrastructure/Authorization/AclGrantService`): update-else-insert grant / clear-bit+delete-empty revoke + **synchronous `recompute_user_perms`** (Phase 6's no-async-revocation stance). Promotion IS the grant (`SetGroupMemberRole`); leave/kick revoke it. Global staff hold moderate everywhere → may MANAGE any group, but chat/DM reads stay participant-gated (outside ACL). Deliberate.
- **Use cases** (Result, 404→403→422; ~30 handlers under `Application/{Friends,Blocks,Groups,Messaging,Notifications,Privacy,Presence}`): shared `SocialInteractionGate` (block-either-direction + per-kind privacy audience, friends-audience checks accepted friendship) and `Notifier` (durable row + `NotificationCreated` outbox event in one step). Send-message re-checks block/privacy on EVERY direct send; sender's own read marker advances with their message. `OpenDirectConversation` catches the direct_key unique race and returns the winner. DeleteMessage: sender, or manage-gate in GROUP chats only (staff remove, never read). `UserReader` = ADO peek at `forum_identity.users` (non-active accounts socially invisible → 404 before any 403). Consumer: Identity's `UserBlockedIntegrationEvent` → drops the banned account's pending requests+invites both directions. 26 endpoints under `/api/social/*` (all RequireAuthorization; presence batch `?userIds=` ≤100 mirror of reaction-batch; conversation list is the ONE documented no-cursor list — unstable last-activity order, hard cap 200; every other list keysets on ULID id = the cursor).
- **Realtime (ADR 0011, Bootstrap edits):** `RealtimeNotification` → `(Payload, RealtimeVisibility, Routes: SubscriptionView[])` — **routes-as-data**, `SubscriptionSet.MatchesAny` is pure set intersection; `ViewKind` += `Group`/`Conversation` (subscribe always accepted, authorized per push — category precedent; `user` stays self-only). Dispatcher gate per kind: Category → `IContentVisibility` (unchanged), Conversation → **`ISocialVisibility.IsConversationParticipantAsync`** (ONE method covers DMs AND groups since conv id == group id), TargetUsers → no check (user-view routes only, self-gated at subscribe). 14 social events wired in `RealtimeEventMap` (22 total); wire envelope UNCHANGED — social events: `categoryId=null`, `parentId`=container, new entities `friendship/group/group_member/group_invite/message/notification`. DMs route to conversation+both user views (badge path); group messages to conversation+group views (**no per-member fan-out** — documented tradeoff). Not wired: GroupCreated, presence (never on the bus).
- **Files:** enum `Dm`→**`Message`** (wire "message"; no live 'dm' rows existed — it was a 422 stub) + **`GroupIcon`** ("group_icon", replace-arm). New Contracts port **`ISocialAuthorization`** (Message: sender-only, tombstone 422; GroupIcon: owner-or-moderate@group) consumed by Attach/Detach + **the READ gap is CLOSED**: `GetFileDownload`/`ListTargetFiles` gate message-attached files through `AuthorizeFileReadAsync` (active participant; anonymous 403) — ULID-unguessability stays the documented tradeoff ONLY for public content; group icons anonymous like avatars. Consumers: `MessageDeleted`/`GroupDeleted` detach → orphan sweep. Files→Social.Contracts project ref (graph: Identity ← Social ← Files, acyclic).
- **Seeding:** SeedPlan social counts as init-props — **Development only** (4+1 friendships, book-club public/staff-room private, 1 invite, alice↔bob DM, 12 messages, 2 unread bell rows; bob = book-club admin **through the real `IAclGrantService`**); **Benchmark = 0, BLOCKED on Hubert** (Decision 3) — SocialSeeder no-ops. New streams friendship/group/group-invite/conversation/message/social-notification (group chats reuse group ids).
- **Guardrails:** ModuleBoundaryTests — Social touches Identity via Contracts only, knows nothing of Content/Files/Engagement; only Files may consume Social (Contracts); Social domain purity. New `Forum.Modules.Social.Tests` (25 unit: friendship transitions, tombstone+direct-key invariants, privacy normalization, gate indistinguishability, owner-leave/kick/transfer/promotion handlers, SendFriendRequest ordering). Api.Tests rewritten for routes/visibility (51). Files.Tests += message/group-icon authorize+replace (42).
- **Verified green:** `dotnet build` · `dotnet format --verify-no-changes` · **238 tests** (25 Social, 51 Api, 42 Files, 30 Identity, 26 Content, 24 Engagement, 2 arch, **38 integration** — the full pre-existing E2E surface incl. migrations of the new schema+views on live Postgres, SeedFlowTests now exercising SocialSeeder+ACL grant, realtime suite on the refactored hub). **NOT yet done: dedicated `SocialFlowTests` E2E** (friend→accept→DM→WS push; group create→invite→message→push; block/privacy 403s; group-icon replace + message-image attach/detach-on-delete + read gate; presence online→offline) — next session, spec in PHASE-11-SOCIAL-PROGRESS.md. Work uncommitted — review then commit.

**Phase 5 — Social: status CONFIRMED (2026-07-16, superseded by the Phase 11 entry above; original scope note kept for history).** Hubert (Architecture B) confirmed he is building the same relationship surface — including **groups** (gomx already added groups, so B-parity now requires them here too, beyond the original friends+DM-only scope from `REQUIREMENTS-AND-ASSUMPTIONS.md` §1/`PHASE-9-10-ENTERPRISE-PLAN.md` §10e). Scope: friendships (request/accept/remove), direct messages, and groups (create/invite/join/leave/membership roles) — **everything that needs it pushed live over the existing WebSocket hub** (invitations, messages, membership changes; not just polled after the fact). Build split across two separate Fable 5 sessions: one scoped to backend (`Forum.Modules.Social` + realtime wiring, mirroring the Phase 7 hub pattern), one scoped to frontend (wiring the existing `/social` mock page — currently a zero-fetch UI-only preview — to the real endpoints). Detail: `docs/architecture/POST-9C-ROADMAP.md`.

**Next: (1) finish Social backend E2E (`SocialFlowTests`, spec in `docs/architecture/PHASE-11-SOCIAL-PROGRESS.md`), (2) Social frontend session (wire the `/social` mock to the real `/api/social/*` + new WS views `group`/`conversation` + bell), (3) Redis session (now ALSO swaps `IPresenceStore`), all detailed in `docs/architecture/POST-9C-ROADMAP.md`. §10d step 6 is closed by documentation, not a re-run** (10d landed 2026-07-15, before 9c's first archive 2026-07-16 — no true "before" snapshot exists; the qualitative Tempo evidence + SPA N+1 fix already recorded in the 10d block above is the accepted record, nothing is being rolled back to manufacture one). **The comparative run against Architecture B is blocked** — Hubert hasn't landed his side's changes yet (notably groups) — revisit once he does. `docs/specs/forum-spec.md` and `docs/architecture/PROPOZYCJA-UJEDNOLICENIA-A-B.md` are real, never-written documentation debt (cited everywhere as the authoritative shared A/B contract) — write them once both sides' scope (Social incl. groups, Redis-or-not, seed volumes) is locked with Hubert. Frontend gaps flagged in `frontend/README.md`: tag autocomplete mocked (needs `GET /api/content/tags?query=` — NOTE: the endpoint exists now, `SuggestTagsEndpoint`), profile activity needs feed-by-owner filter, admin UI deliberately unbuilt.**

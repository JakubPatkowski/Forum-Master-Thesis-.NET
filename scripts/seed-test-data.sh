#!/usr/bin/env bash
# Seed deterministic data for load tests (users, categories, threads, comments).
set -euo pipefail
echo "TODO: call POST /api/v1/dev/seed or run the SeedDataStartupTask in a Job."
